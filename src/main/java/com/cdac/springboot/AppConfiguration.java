package com.cdac.springboot;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import com.cdac.login.LoginStatus;
import com.cdac.login.RegisterStatus;
import com.cdac.login.Student;

@Configuration
public class AppConfiguration {
	
/*	@Bean
	public LoginStatus getLoginStatus(String statusMessage,boolean status, Student student){
		return new LoginStatus(statusMessage, status, student);
	}
	
	@Bean
	public RegisterStatus getRegisterStatus(String message,	boolean status){
		return new RegisterStatus(message, status);
	}*/

}